# TV Projects

Device-specific TV projects belong in this directory.

Use one stable lowercase platform marker per project, for example:

- `rokutv`
- `appletv`
- `androidtv`

Each project should include its own README, build notes, interaction model, and license.
